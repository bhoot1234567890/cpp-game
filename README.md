# CPP Game

Small terminal-based maze game written in C++ using `yaml-cpp` for dialogues.

Build:

```bash
make
```

Run:

```bash
./game
```

Dependencies:
- yaml-cpp (install via Homebrew: `brew install yaml-cpp`)

This repository was prepared for publishing to GitHub.
